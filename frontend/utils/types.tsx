
export type Driver = {
    name: string;
    location: { latitude: number; longitude: number };
}

export type Passenger = {
    name: string;
    location: { latitude: number; longitude: number };
}
