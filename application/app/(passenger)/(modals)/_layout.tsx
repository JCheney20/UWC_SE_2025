import { Slot } from "expo-router";

// No need to for a navigator as the layout before sets the needed options
// Also, not rendering another navigator allows for the modals to be seen on top of the tabs
export default <Slot />
